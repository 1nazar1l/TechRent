@model IEnumerable<TechRent.Models.Entities.Equipment>
@{
    ViewData["Title"] = "Каталог";
    ViewData["css"] = "catalog";
    ViewData["nav_pos"] = "left";
    ViewData["header"] = "with_search";
}

<main class="main-content">
    <div class="wrapper">
        <!-- Breadcrumb & Title -->
        <div class="breadcrumb">
            <a href="#" class="breadcrumb-link">Home</a>
            <span class="material-symbols-outlined">chevron_right</span>
            <span class="breadcrumb-current">Catalog</span>
        </div>
        <h1 class="page-title">Equipment Catalog</h1>
        <p class="page-description">Browse our premium selection of construction machinery.</p>

        <!-- Catalog -->
        <div class="catalog">
            <!-- Filters Sidebar -->
            <aside class="filters">
                <div class="filters-header">
                    <h2 class="filters-title">Filters</h2>
                    <button class="reset-button" onclick="location.href='@Url.Action("Index")'">Reset All</button>
                </div>

                <form method="get" id="filterForm">
                    <!-- Search Filter -->
                    <div class="filters-group">
                        <label class="filter-label">Keywords</label>
                        <div class="filter-search">
                            <span class="filter-search-icon material-symbols-outlined">search</span>
                            <input type="text" name="searchString" class="filter-search-input" placeholder="Model, ID, or Type" value="@ViewBag.SearchString">
                        </div>
                    </div>

                    <!-- Categories -->
                    <div class="filters-group">
                        <label class="filter-label">Category</label>
                        <div class="checkbox-list">
                            @foreach (var item in ViewBag.Categories)
                            {
                                <label class="checkbox-item">
                                    <span class="checkbox-box">
                                        <input type="checkbox" name="categoryIds" class="checkbox-input" value="@item.Category.Id" 
                                               @(ViewBag.SelectedCategoryIds.Contains(item.Category.Id) ? "checked" : "")>
                                        <span class="checkbox-check material-symbols-outlined">check</span>
                                    </span>
                                    <span class="checkbox-text">@item.Category.Name (@item.Count)</span>
                                </label>
                            }
                        </div>
                    </div>

                    <!-- Price Range -->
                    <div class="filters-group">
                        <label class="filter-label">Daily Rate</label>
                        <div class="price-slider">
                            <div class="price-inputs">
                                <div class="price-field">
                                    <span class="price-currency">$</span>
                                    <input type="number" name="minPrice" class="price-input" value="@ViewBag.MinPrice" placeholder="Min">
                                </div>
                                <span class="price-separator">-</span>
                                <div class="price-field">
                                    <span class="price-currency">$</span>
                                    <input type="number" name="maxPrice" class="price-input" value="@ViewBag.MaxPrice" placeholder="Max">
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- Availability -->
                    <div class="filters-group">
                        <label class="toggle">
                            <span class="toggle-text">In Stock Only</span>
                            <div class="relative">
                                <input type="checkbox" name="inStockOnly" class="toggle-input" value="true" @(ViewBag.InStockOnly == true ? "checked" : "")>
                                <div class="toggle-switch"></div>
                            </div>
                        </label>
                    </div>

                    <!-- Hidden fields -->
                    <input type="hidden" name="sortOrder" value="@ViewBag.SortOrder" />
                    <input type="hidden" name="page" value="1" />
                    <input type="hidden" name="pageSize" value="@ViewBag.PageSize" />

                    <!-- Submit button -->
                    <button type="submit" class="save-button" style="width: 100%; margin-top: 16px; justify-content: center;">
                        <span class="material-symbols-outlined">filter_alt</span>
                        Apply Filters
                    </button>
                </form>
            </aside>

            <!-- Products Section -->
            <section class="products">
                <!-- Toolbar -->
                <div class="toolbar">
                    <div class="results">
                        <span class="results-number">@ViewBag.TotalItems</span>
                        <span class="results-text">items found</span>
                    </div>

                    <div class="sort">
                        <div class="sort-field">
                            <span class="sort-label">Sort by:</span>
                            <select class="sort-select" onchange="changeSortOrder(this.value)">
                                <option value="recommended" selected="@(ViewBag.SortOrder == "recommended")">Recommended</option>
                                <option value="price_asc" selected="@(ViewBag.SortOrder == "price_asc")">Price: Low to High</option>
                                <option value="price_desc" selected="@(ViewBag.SortOrder == "price_desc")">Price: High to Low</option>
                                <option value="rating" selected="@(ViewBag.SortOrder == "rating")">Top Rated</option>
                                <option value="newest" selected="@(ViewBag.SortOrder == "newest")">Newest</option>
                            </select>
                        </div>

                        <div class="divider"></div>

                        <div class="view-toggle">
                            <button class="view-button view-button-active">
                                <span class="material-symbols-outlined">grid_view</span>
                            </button>
                            <button class="view-button">
                                <span class="material-symbols-outlined">view_list</span>
                            </button>
                        </div>
                    </div>
                </div>

                <!-- Products Grid -->
                <div class="products-grid">
                    @foreach (var item in Model)
                    {
                        var averageRating = item.Reviews != null && item.Reviews.Any() 
                            ? item.Reviews.Average(r => r.Rating).ToString("F1") 
                            : "0.0";
                        var reviewCount = item.Reviews?.Count ?? 0;
                        
                        <article class="product-card">
                            <div class="product-badge">
                                @if (item.AvailableQuantity > 5)
                                {
                                    <span class="badge badge-available">Available</span>
                                }
                                else if (item.AvailableQuantity > 0)
                                {
                                    <span class="badge badge-lowstock">Low Stock</span>
                                }
                                else
                                {
                                    <span class="badge badge-outofstock">Out of Stock</span>
                                }
                            </div>
                            <button class="favorite-button">
                                <span class="material-symbols-outlined">favorite</span>
                            </button>
                            <div class="product-image">
                                @if (!string.IsNullOrEmpty(item.ImageUrl))
                                {
                                    <img src="@item.ImageUrl" alt="@item.Name">
                                }
                                else
                                {
                                    <div style="width: 100%; height: 100%; background: #f1f5f9; display: flex; align-items: center; justify-content: center;">
                                        <span class="material-symbols-outlined" style="font-size: 48px; color: #94a3b8;">construction</span>
                                    </div>
                                }
                                <div class="image-overlay"></div>
                            </div>
                            <div class="product-details">
                                <div class="product-header">
                                    <h3 class="product-title">@item.Name</h3>
                                    <p class="product-description">@(item.Description ?? "No description available")</p>
                                </div>
                                
                                <!-- Rating -->
                                <div style="display: flex; align-items: center; gap: 4px; margin-bottom: 8px;">
                                    @{
                                        double ratingValue = 0;
                                        if (item.Reviews != null && item.Reviews.Any())
                                        {
                                            ratingValue = item.Reviews.Average(r => r.Rating);
                                        }
                                        int roundedRating = (int)Math.Round(ratingValue);
                                    }
    
                                    @for (int i = 0; i < 5; i++)
                                    {
                                        if (i < roundedRating)
                                        {
                                            <span class="material-symbols-outlined" style="color: #fbbf24; font-size: 18px; font-variation-settings: 'FILL' 1;">star</span>
                                        }
                                        else
                                        {
                                            <span class="material-symbols-outlined" style="color: #cbd5e1; font-size: 18px; font-variation-settings: 'FILL' 1;">star</span>
                                        }
                                    }
                                    <span style="font-size: 14px; color: #64748b; margin-left: 4px;">(@(item.Reviews?.Count ?? 0) reviews)</span>
                                </div>
                                
                                <div class="specs">
                                    <div class="spec">
                                        <span class="spec-icon material-symbols-outlined">category</span>
                                        <span>@(item.Category?.Name ?? "Uncategorized")</span>
                                    </div>
                                    <div class="spec">
                                        <span class="spec-icon material-symbols-outlined">inventory</span>
                                        <span>@item.AvailableQuantity in stock</span>
                                    </div>
                                </div>
                                
                                <div class="product-footer">
                                    <div class="price">
                                        <span class="price-label">Daily Rate</span>
                                        <span class="price-value">$@item.PricePerDay<span class="price-period">/day</span></span>
                                    </div>
                                    @if (item.AvailableQuantity > 0)
                                    {
                                        <button class="book-button" onclick="location.href='@Url.Action("Details", "Equipment", new { id = item.Id })'">Book Now</button>
                                    }
                                    else
                                    {
                                        <button class="book-button" disabled style="opacity: 0.5; cursor: not-allowed;">Out of Stock</button>
                                    }
                                </div>
                            </div>
                        </article>
                    }
                </div>

                <!-- Pagination -->
                <div class="pagination">
                    <nav class="pagination-list">
                        @if (ViewBag.CurrentPage > 1)
                        {
                            <a href="@Url.Action("Index", new { 
                                page = ViewBag.CurrentPage - 1, 
                                pageSize = ViewBag.PageSize,
                                searchString = ViewBag.SearchString,
                                categoryIds = ViewBag.SelectedCategoryIds,
                                minPrice = ViewBag.MinPrice,
                                maxPrice = ViewBag.MaxPrice,
                                inStockOnly = ViewBag.InStockOnly,
                                sortOrder = ViewBag.SortOrder
                            })" class="pagination-link">
                                <span class="sr-only">Previous</span>
                                <span class="material-symbols-outlined">chevron_left</span>
                            </a>
                        }

                        @for (int i = 1; i <= ViewBag.TotalPages; i++)
                        {
                            if (i == ViewBag.CurrentPage)
                            {
                                <a href="#" class="pagination-link pagination-link-active">@i</a>
                            }
                            else
                            {
                                <a href="@Url.Action("Index", new { 
                                    page = i, 
                                    pageSize = ViewBag.PageSize,
                                    searchString = ViewBag.SearchString,
                                    categoryIds = ViewBag.SelectedCategoryIds,
                                    minPrice = ViewBag.MinPrice,
                                    maxPrice = ViewBag.MaxPrice,
                                    inStockOnly = ViewBag.InStockOnly,
                                    sortOrder = ViewBag.SortOrder
                                })" class="pagination-link">@i</a>
                            }
                        }

                        @if (ViewBag.CurrentPage < ViewBag.TotalPages)
                        {
                            <a href="@Url.Action("Index", new { 
                                page = ViewBag.CurrentPage + 1, 
                                pageSize = ViewBag.PageSize,
                                searchString = ViewBag.SearchString,
                                categoryIds = ViewBag.SelectedCategoryIds,
                                minPrice = ViewBag.MinPrice,
                                maxPrice = ViewBag.MaxPrice,
                                inStockOnly = ViewBag.InStockOnly,
                                sortOrder = ViewBag.SortOrder
                            })" class="pagination-link">
                                <span class="sr-only">Next</span>
                                <span class="material-symbols-outlined">chevron_right</span>
                            </a>
                        }
                    </nav>
                </div>
            </section>
        </div>
    </div>
</main>

<script>
    function changeSortOrder(sortOrder) {
        var url = new URL(window.location.href);
        url.searchParams.set('sortOrder', sortOrder);
        url.searchParams.set('page', 1);
        window.location.href = url.toString();
    }
</script>