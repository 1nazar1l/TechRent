@model IEnumerable<TechRent.Models.Entities.Equipment>
@{
    ViewData["Title"] = "Админ панель";
    ViewData["css"] = "admin";
}

<main class="main-content">
    <div class="wrapper">
        <!-- Breadcrumb & Title -->
        <div class="breadcrumb">
            <a href="#" class="breadcrumb-link">Админ</a>
            <span class="material-symbols-outlined">chevron_right</span>
            <span class="breadcrumb-current">Управление оборудованием</span>
        </div>

        <div class="page-header">
            <h1 class="page-title">Управление оборудованием</h1>
            <button class="add-button" onclick="location.href='@Url.Action("Create", "Admin")'">
                <span class="material-symbols-outlined">add</span>
                Добавить оборудование
            </button>
        </div>

        <!-- Admin Layout -->
        <div class="admin-layout">
            <!-- Filters Sidebar -->
            <aside class="admin-filters">
                <div class="filters-header">
                    <h2 class="filters-title">Фильтры</h2>
                    <button class="reset-button" onclick="location.href='@Url.Action("Index")'">Сбросить</button>
                </div>

                <form method="get" id="filterForm">
                    <!-- Search Filter -->
                    <div class="filters-group">
                        <label class="filter-label">Поиск</label>
                        <div class="filter-search">
                            <span class="filter-search-icon material-symbols-outlined">search</span>
                            <input type="text" name="searchString" class="filter-search-input" placeholder="Название, ID, категория..." value="@ViewBag.SearchString">
                        </div>
                    </div>

                    <!-- Category Filter -->
                    <div class="filters-group">
                        <label class="filter-label">Категория</label>
                        <div class="filter-select">
                            <select name="categoryId" class="filter-select-input">
                                <option value="" selected="@(ViewBag.SelectedCategoryId == null)">Все категории</option>
                                @foreach (var category in ViewBag.Categories)
                                {
                                    <option value="@category.Id" selected="@(ViewBag.SelectedCategoryId == category.Id)">@category.Name</option>
                                }
                            </select>
                            <span class="filter-select-icon material-symbols-outlined">expand_more</span>
                        </div>
                    </div>

                    <!-- Status Filter -->
                    <div class="filters-group">
                        <label class="filter-label">Статус</label>
                        <div class="checkbox-list">
                            <label class="checkbox-item">
                                <span class="checkbox-box">
                                    <input type="checkbox" name="availableOnly" class="checkbox-input" value="true" @(ViewBag.AvailableOnly == true ? "checked" : "")>
                                    <span class="checkbox-check material-symbols-outlined">check</span>
                                </span>
                                <span class="checkbox-text">В наличии</span>
                            </label>
                            <label class="checkbox-item">
                                <span class="checkbox-box">
                                    <input type="checkbox" name="lowStockOnly" class="checkbox-input" value="true" @(ViewBag.LowStockOnly == true ? "checked" : "")>
                                    <span class="checkbox-check material-symbols-outlined">check</span>
                                </span>
                                <span class="checkbox-text">Мало на складе</span>
                            </label>
                            <label class="checkbox-item">
                                <span class="checkbox-box">
                                    <input type="checkbox" name="outOfStockOnly" class="checkbox-input" value="true" @(ViewBag.OutOfStockOnly == true ? "checked" : "")>
                                    <span class="checkbox-check material-symbols-outlined">check</span>
                                </span>
                                <span class="checkbox-text">Нет в наличии</span>
                            </label>
                        </div>
                    </div>

                    <!-- Price Range -->
                    <div class="filters-group">
                        <label class="filter-label">Диапазон цен ($)</label>
                        <div class="price-inputs">
                            <div class="price-field">
                                <span class="price-currency">$</span>
                                <input type="number" name="minPrice" class="price-input" value="@ViewBag.MinPrice" placeholder="Мин">
                            </div>
                            <span class="price-separator">-</span>
                            <div class="price-field">
                                <span class="price-currency">$</span>
                                <input type="number" name="maxPrice" class="price-input" value="@ViewBag.MaxPrice" placeholder="Макс">
                            </div>
                        </div>
                    </div>

                    <!-- Rating Filter -->
                    <div class="filters-group">
                        <label class="filter-label">Минимальный рейтинг</label>
                        <div class="filter-select">
                            <select name="rating" class="filter-select-input">
                                <option value="" selected="@(string.IsNullOrEmpty(ViewBag.SelectedRating))">Все оценки</option>
                                <option value="5stars" selected="@(ViewBag.SelectedRating == "5stars")">5 звёзд</option>
                                <option value="4+stars" selected="@(ViewBag.SelectedRating == "4+stars")">4+ звезды</option>
                                <option value="3+stars" selected="@(ViewBag.SelectedRating == "3+stars")">3+ звезды</option>
                                <option value="2+stars" selected="@(ViewBag.SelectedRating == "2+stars")">2+ звезды</option>
                            </select>
                            <span class="filter-select-icon material-symbols-outlined">expand_more</span>
                        </div>
                    </div>

                    <!-- Availability Toggle -->
                    <div class="filters-group">
                        <label class="toggle">
                            <span class="toggle-text">Только в наличии</span>
                            <div class="relative">
                                <input type="checkbox" name="availableOnly" class="toggle-input" value="true" @(ViewBag.AvailableOnly == true ? "checked" : "")>
                                <div class="toggle-switch"></div>
                            </div>
                        </label>
                    </div>

                    <!-- Hidden fields for pagination -->
                    <input type="hidden" name="page" value="1" />
                    <input type="hidden" name="pageSize" value="@ViewBag.PageSize" />

                    <!-- Submit button -->
                    <button type="submit" class="save-button" style="width: 100%; margin-top: 16px; justify-content: center;">
                        <span class="material-symbols-outlined">filter_alt</span>
                        Применить фильтры
                    </button>
                </form>
            </aside>
            <!-- Table Section -->
            <section class="table-section">
                <!-- Toolbar -->
                <div class="table-toolbar">
                    <div class="results">
                        <span class="results-number">@Model.Count()</span>
                        <span class="results-text">позиций всего</span>
                    </div>

                    <div class="table-controls">
                        <div class="sort-field">
                            <span class="sort-label">Показать:</span>
                            <select class="sort-select" onchange="changePageSize(this.value)">
                                <option value="10" selected="@(ViewBag.PageSize == 10)">10 записей</option>
                                <option value="25" selected="@(ViewBag.PageSize == 25)">25 записей</option>
                                <option value="50" selected="@(ViewBag.PageSize == 50)">50 записей</option>
                                <option value="100" selected="@(ViewBag.PageSize == 100)">100 записей</option>
                            </select>
                        </div>

                        <script>
                            function changePageSize(pageSize) {
                                var url = new URL(window.location.href);
                                url.searchParams.set('pageSize', pageSize);
                                url.searchParams.set('page', 1);
                                window.location.href = url.toString();
                            }
                        </script>

                        <div class="divider"></div>

                        <div class="export-buttons">
                            <button class="icon-button" title="Экспорт CSV">
                                <span class="material-symbols-outlined">download</span>
                            </button>
                            <button class="icon-button" title="Печать">
                                <span class="material-symbols-outlined">print</span>
                            </button>
                        </div>
                    </div>
                </div>

                <!-- Table Wrapper with horizontal scroll -->
                <div class="table-wrapper">
                    <table class="admin-table">
                        <thead>
                            <tr>
                                <th class="checkbox-cell">
                                    <label class="checkbox-box">
                                        <input type="checkbox" class="checkbox-input" id="select-all">
                                        <span class="checkbox-check material-symbols-outlined">check</span>
                                    </label>
                                </th>
                                <th>ID</th>
                                <th>Фото</th>
                                <th>Название</th>
                                <th>Категория</th>
                                <th>Цена/день</th>
                                <th>Склад</th>
                                <th>Статус</th>
                                <th>Рейтинг</th>
                                <th>Действия</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                var averageRating = item.Reviews != null && item.Reviews.Any()
                                ? item.Reviews.Average(r => r.Rating).ToString("F1")
                                : "—";
                                var reviewCount = item.Reviews?.Count ?? 0;

                                <tr class="@(item.AvailableQuantity == 0 ? "inactive-row" : "")">
                                    <td class="checkbox-cell">
                                        <label class="checkbox-box">
                                            <input type="checkbox" class="checkbox-input">
                                            <span class="checkbox-check material-symbols-outlined">check</span>
                                        </label>
                                    </td>
                                    <td class="id-cell">@item.Id.ToString()</td>
                                    <td class="image-cell">
                                        <div class="table-image">
                                            @if (!string.IsNullOrEmpty(item.ImageUrl))
                                            {
                                                <img src="@item.ImageUrl" alt="@item.Name">
                                            }
                                            else
                                            {
                                                <div class="no-image">Нет фото</div>
                                            }
                                        </div>
                                    </td>
                                    <td class="name-cell">
                                        <div class="cell-content">
                                            <strong>@item.Name</strong>
                                            @if (!string.IsNullOrEmpty(item.Description))
                                            {
                                                <span class="cell-description">@item.Description</span>
                                            }
                                        </div>
                                    </td>
                                    <td>@item.Category?.Name</td>
                                    <td class="price-cell">$@item.PricePerDay.ToString("F0")</td>
                                    <td class="stock-cell">
                                        @if (item.AvailableQuantity > 5)
                                        {
                                            <span class="stock-badge stock-available">@item.AvailableQuantity в наличии</span>
                                        }
                                        else if (item.AvailableQuantity > 0)
                                        {
                                            <span class="stock-badge stock-low">@item.AvailableQuantity в наличии</span>
                                        }
                                        else
                                        {
                                            <span class="stock-badge stock-out">0 в наличии</span>
                                        }
                                    </td>
                                    <td class="status-cell">
                                        @if (item.IsAvailable)
                                        {
                                            <span class="status-badge status-active">Активен</span>
                                        }
                                        else
                                        {
                                            <span class="status-badge status-inactive">Неактивен</span>
                                        }
                                    </td>
                                    <td class="rating-cell">
                                        <div class="rating">
                                            @if (averageRating != "—")
                                            {
                                                <div style="display: flex; align-items: center; gap: 4px;">
                                                    <span class="star-icon material-symbols-outlined" style="color: #fbbf24;">star</span>
                                                    <span style="font-weight: 600;">@averageRating</span>
                                                    <span style="color: #64748b; font-size: 12px;">(@reviewCount)</span>
                                                </div>
                                            }
                                            else
                                            {
                                                <span class="text-muted">—</span>
                                            }
                                        </div>
                                    </td>
                                    <td class="actions-cell">
                                        <div class="action-buttons">
                                            <button class="action-button edit-button" title="Редактировать" onclick="location.href='@Url.Action("Edit", "Admin", new { id = item.Id })'">
                                                <span class="material-symbols-outlined">edit</span>
                                            </button>
                                            <button class="action-button delete-button" title="Удалить" onclick="location.href='@Url.Action("Delete", "Admin", new { id = item.Id })'">
                                                <span class="material-symbols-outlined">delete</span>
                                            </button>
                                            <button class="action-button view-button" title="Просмотр" onclick="location.href='@Url.Action("Index", "Product", new { id = item.Id })'">
                                                <span class="material-symbols-outlined">visibility</span>
                                            </button>
                                        </div>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>

                <!-- Pagination -->
                <div class="pagination">
                    <nav class="pagination-list">
                        @if (ViewBag.CurrentPage > 1)
                        {
                            <a href="@Url.Action("Index", new { 
                                page = ViewBag.CurrentPage - 1, 
                                pageSize = ViewBag.PageSize,
                                searchString = ViewBag.SearchString,
                                categoryId = ViewBag.SelectedCategoryId,
                                availableOnly = ViewBag.AvailableOnly,
                                lowStockOnly = ViewBag.LowStockOnly,
                                outOfStockOnly = ViewBag.OutOfStockOnly,
                                minPrice = ViewBag.MinPrice,
                                maxPrice = ViewBag.MaxPrice,
                                rating = ViewBag.SelectedRating
                            })" class="pagination-link">
                                <span class="sr-only">Назад</span>
                                <span class="material-symbols-outlined">chevron_left</span>
                            </a>
                        }

                        @for (int i = 1; i <= ViewBag.TotalPages; i++)
                        {
                            if (i == ViewBag.CurrentPage)
                            {
                                <a href="#" class="pagination-link pagination-link-active">@i</a>
                            }
                            else
                            {
                                <a href="@Url.Action("Index", new { 
                                    page = i, 
                                    pageSize = ViewBag.PageSize,
                                    searchString = ViewBag.SearchString,
                                    categoryId = ViewBag.SelectedCategoryId,
                                    availableOnly = ViewBag.AvailableOnly,
                                    lowStockOnly = ViewBag.LowStockOnly,
                                    outOfStockOnly = ViewBag.OutOfStockOnly,
                                    minPrice = ViewBag.MinPrice,
                                    maxPrice = ViewBag.MaxPrice,
                                    rating = ViewBag.SelectedRating
                                })" class="pagination-link">@i</a>
                            }
                        }

                        @if (ViewBag.CurrentPage < ViewBag.TotalPages)
                        {
                            <a href="@Url.Action("Index", new { 
                                page = ViewBag.CurrentPage + 1, 
                                pageSize = ViewBag.PageSize,
                                searchString = ViewBag.SearchString,
                                categoryId = ViewBag.SelectedCategoryId,
                                availableOnly = ViewBag.AvailableOnly,
                                lowStockOnly = ViewBag.LowStockOnly,
                                outOfStockOnly = ViewBag.OutOfStockOnly,
                                minPrice = ViewBag.MinPrice,
                                maxPrice = ViewBag.MaxPrice,
                                rating = ViewBag.SelectedRating
                            })" class="pagination-link">
                                <span class="sr-only">Вперёд</span>
                                <span class="material-symbols-outlined">chevron_right</span>
                            </a>
                        }
                    </nav>
                </div>
            </section>
        </div>
    </div>
</main>

<style>
    .text-muted {
        color: #94a3b8;
        font-style: italic;
    }

    .rating {
        display: flex;
        align-items: center;
    }

    .star-icon {
        font-size: 18px;
        font-variation-settings: 'FILL' 1;
    }
</style>