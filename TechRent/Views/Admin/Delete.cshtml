@model TechRent.Models.Entities.Equipment
@{
    ViewData["Title"] = "Удалить оборудование";
    ViewData["css"] = "admin";
}

<main class="main-content">
    <div class="wrapper">
        <div class="breadcrumb">
            <a asp-action="Index" class="breadcrumb-link">Админ</a>
            <span class="material-symbols-outlined">chevron_right</span>
            <span class="breadcrumb-current">Удалить оборудование</span>
        </div>

        <div class="delete-container">
            <div class="delete-card">
                <div class="delete-icon">
                    <span class="material-symbols-outlined" style="font-size: 48px; color: #ef4444;">warning</span>
                </div>

                <h1 class="delete-title">Удалить оборудование</h1>

                <p class="delete-text">
                    Вы уверены, что хотите удалить <strong>"@Model.Name"</strong>?
                    Это действие нельзя отменить.
                </p>

                @if (!string.IsNullOrEmpty(Model.ImageUrl))
                {
                    <div class="delete-image">
                        <img src="@Model.ImageUrl" alt="@Model.Name" style="max-width: 200px; border-radius: 8px;">
                    </div>
                }

                <div class="delete-details">
                    <div class="detail-item">
                        <span class="detail-label">ID:</span>
                        <span class="detail-value">#@Model.Id.ToString("D3")</span>
                    </div>
                    <div class="detail-item">
                        <span class="detail-label">Тип:</span>
                        <span class="detail-value">@Model.Category?.Name</span>
                    </div>
                    <div class="detail-item">
                        <span class="detail-label">Цена:</span>
                        <span class="detail-value">$@Model.PricePerDay/день</span>
                    </div>
                    <div class="detail-item">
                        <span class="detail-label">Склад:</span>
                        <span class="detail-value">@Model.AvailableQuantity шт.</span>
                    </div>
                </div>

                <form asp-action="Delete" method="post">
                    <input type="hidden" asp-for="Id" />

                    <div class="delete-actions">
                        <button type="submit" class="delete-button-confirm">
                            <span class="material-symbols-outlined">delete</span>
                            Удалить навсегда
                        </button>

                        <a asp-action="Index" class="delete-button-cancel">
                            <span class="material-symbols-outlined">close</span>
                            Отмена
                        </a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</main>